name: Audit Fisher

on:
  push:
    branches:
      - audit/fisher
  workflow_dispatch:

permissions:
  contents: write

jobs:
  snapshot:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Generate Fisher snapshot
        run: node scripts/report-fisher-current.mjs
      - name: Commit snapshot
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add reports/fisher-current.json reports/fisher-current.md
          if ! git diff --cached --quiet; then
            git commit -m "Capture Fisher audit snapshot"
            git push
          fi
